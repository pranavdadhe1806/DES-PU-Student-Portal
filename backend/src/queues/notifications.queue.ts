// TODO: Notifications BullMQ queue
// - Job: send email notification
// - Job: send in-app push notification via Socket.io
// - Keeps route handlers under 100ms — never send emails inline

export {};
