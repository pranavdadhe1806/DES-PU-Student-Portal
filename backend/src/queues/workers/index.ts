// TODO: BullMQ worker processes
// - fileProcessingWorker: handles thumbnail generation, PDF indexing
// - notificationsWorker: handles email + push notification delivery
// - aiSummaryWorker: handles Claude API calls for summaries/tags
// Workers run in separate processes to avoid blocking the main event loop

export {};
