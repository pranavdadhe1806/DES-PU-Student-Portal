// TODO: User routes
// GET  /users/:id        → Get public user profile
// PATCH /users/:id       → Update editable profile fields (bio, username, photo)
// GET  /users/:id/badges → Get user's earned badges

import { Router } from 'express';

const router = Router();

// TODO: Mount user controllers here

export default router;
