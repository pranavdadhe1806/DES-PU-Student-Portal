// TODO: Project Marketplace routes
// GET    /projects             → Browse/explore projects (paginated, filterable)
// POST   /projects             → Create a new project listing
// GET    /projects/:id         → Get project details
// PATCH  /projects/:id         → Update project (leader only)
// POST   /projects/:id/apply   → Apply to join a project
// GET    /projects/:id/members → List project team members

import { Router } from 'express';

const router = Router();

// TODO: Mount project controllers here

export default router;
