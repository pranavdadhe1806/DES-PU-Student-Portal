// TODO: Chat service — business logic for messaging
// - getConversations(userId): list conversations with last message preview
// - getMessages(conversationId, pagination): paginated message history
// - sendMessage(data): persist message, emit via Socket.io
// - createGroup(data): create group chat

export {};
