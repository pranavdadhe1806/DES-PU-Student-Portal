// TODO: Notification service — real-time + stored notifications
// - createNotification(data): persist notification to DB
// - sendRealtime(userId, notification): emit via Socket.io
// - markRead(notificationId): mark as read in DB

export {};
