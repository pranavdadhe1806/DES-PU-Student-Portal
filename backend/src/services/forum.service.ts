// TODO: Forum service — business logic for discussion forums
// - getPosts(subjectId, pagination): fetch posts with author, upvote count
// - createPost(data): create post, auto-tag with AI via BullMQ
// - createReply(postId, data): threaded reply (infinitely nested)
// - toggleUpvote(postId, userId): toggle upvote, update XP via queue
// - pinPost(postId): faculty/admin only

export {};
