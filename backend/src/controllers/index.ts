// TODO: Route controllers live here
// Each controller corresponds to a route file:
// - auth.controller.ts    → handles /auth/* requests
// - user.controller.ts    → handles /users/* requests
// - forum.controller.ts   → handles /forums/* requests
// - chat.controller.ts    → handles /chat/* requests
// - resources.controller.ts → handles /resources/* requests
// - projects.controller.ts  → handles /projects/* requests
// - notifications.controller.ts → handles /notifications/* requests
// - admin.controller.ts   → handles /admin/* requests
//
// Controllers are thin — they parse req, call services, send res.
// All business logic lives in /services/

export {};
