// TODO: Shared utility functions
// - generateId(): CUID/UUID generation
// - slugify(str): convert string to URL slug
// - paginate(page, limit): calculate skip/take for Prisma
// - formatApiError(err): standardized error response formatter

export {};
