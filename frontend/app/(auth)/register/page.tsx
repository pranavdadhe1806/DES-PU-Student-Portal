// TODO: Register page
// - DES email + password + confirm password form
// - domainCheck enforced client-side AND server-side
// - Submit → POST /auth/register → redirect to login

export default function RegisterPage() {
  return <div>TODO: Register Page</div>;
}
