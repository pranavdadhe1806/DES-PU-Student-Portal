// TODO: Login page
// - DES email + password form (React Hook Form + Zod)
// - Submit → POST /auth/login → store auth in cookie
// - Redirect to /dashboard on success
// - Link to /register

export default function LoginPage() {
  return <div>TODO: Login Page</div>;
}
