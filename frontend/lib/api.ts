// TODO: Axios/fetch wrapper for all backend API calls
// - Set base URL from NEXT_PUBLIC_API_URL env var
// - Include credentials (cookies) on every request
// - Global error interceptor: handle 401 (redirect to login), 403, 500
// - Export typed API call helpers

export {};
