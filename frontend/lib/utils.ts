// TODO: Shared utility functions for the frontend
// - cn(...classes): merge Tailwind classes with clsx + tailwind-merge
// - formatDate(date): human-readable date formatting
// - formatFileSize(bytes): human-readable file size
// - truncate(str, maxLen): string truncation with ellipsis

export {};
